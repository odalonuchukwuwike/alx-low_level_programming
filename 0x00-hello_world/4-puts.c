#include <stdio.h>
/**
 *main - A program that uses puts function to output a string
 * Return: 0 (Using the puts function to print strings to our output console)
 */

int main (void)
{
	puts("\"Programming is like building a multilingual puzzle");
	return(0);
}
